<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1><%= title %></h1>
  <a href="/new">Post a new message</a>
  <ul>
    <% messages.forEach((message, index) => { %>
      <li>
        <strong><%= message.user %></strong> (<%= message.added.toLocaleString() %>): 
        <%= message.text %> 
        <br>
        <a href="/message/<%= index %>">Open</a>
      </li>
    <% }) %>
  </ul>
</body>
</html>
