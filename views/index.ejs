<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <a href="/new">Post a new message</a>
  <ul>
    <% messages.forEach((message, index) => { %>
      <li>
        <strong><%= message.user %></strong> (<%= message.added.toLocaleString() %>): 
        <%= message.text %> 
        <!-- Optional: Placeholder for details page -->
        <button onclick="alert('Feature coming soon!')">Open</button>
      </li>
    <% }) %>
  </ul>
</body>
</html>
